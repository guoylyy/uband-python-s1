#!/usr/bin/python
# -*- coding: utf-8 -*-
# @author: IPLAY

# 4. 做 day1-note 里面的思考题，用自己的话解释 变量  if 和 <= >= 等符号
#1. what's the meaning of 'number', 'string', 'boolean' in python?
# number: 数
# 整数、长整数、浮点数和复数
# string: 字符串
# 使用单、双和三引号（' “ ’’’）
# 转义符（what\’s your name?）⾏末的单独⼀一个反斜杠表示字符串在下⼀一⾏继续，⽽而不不是开始⼀一个新的⾏
# ⾃自然字符串: 给字符串串加上前缀r或R来指定：r"Newlines are indicated
# by \n"
# Unicode字符串
# 字符串串是不不可变的，按字⾯面意义级连字符串串
# Boolean: 布尔类型
# 在Python中，可以直接用True、False表示布尔值（注意大小写）
# 2. try to describe the meaning of 'if' statement in your mind?
# if语句⽤来检验⼀个条件， 如果条件为真，我们运⾏一块语句（称为 if-块 ）， 否则 我们处理
# 另外⼀块语句（称为 else-块 ）。 else 从句是可选的。
# 3. Why we need > < == >= <= ...etc...?
# 这些都是比较运算符，组建语句的逻辑关系
# 比较运算符可以使用比较两个值，所有的内建类型都支持比较运算。当用运算符比较两个值时，结果是一个逻辑值，不是True，就是False。
