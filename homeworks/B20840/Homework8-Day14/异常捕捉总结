异常捕捉总结

对于try这种异常捕捉一直都是我学习的软肋，不管是在学习C#还是Java得过程中。
比如今天我的homework代码，其实我对于第一段代码里面的e那个部分就想不明白为什么会出现 'dict' object has no attribute 'has_keys' 这样的错误提示
因为按照我的想法，应该类似于dictionary has no keys .....之类的
但是出来的错误提示跟我想象的很明显不一样

第二个try我直接就做一个之前犯过的错误的捕捉，就是缺少变量定义的情况下的看来第二个跟想象的还比较一致